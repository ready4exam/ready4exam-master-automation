name: Count Curriculum Chapters

on:
  workflow_dispatch: {}

jobs:
  count-curriculum:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Count chapters (no external JS file)
        run: |
          echo "📘 Curriculum Summary for Class 5–12"
          echo "--------------------------------------"

          for CLASS in 5 6 7 8 9 10 11 12
          do
            FILE="staticcurriculum/class${CLASS}/curriculum.js"

            if [ ! -f "$FILE" ]; then
              echo "⚠ Class ${CLASS}: curriculum.js not found"
              continue
            fi

            echo ""
            echo "▶ Class ${CLASS}"

            # create a temporary Node script (use single-quoted heredoc so bash won't expand anything)
            cat > /tmp/count_curriculum.js <<'JSCODE'
/*
  Temporary Node script created by GitHub Action.
  It reads process.env.CLASS and requires the curriculum.js file
  relative to the repository root (process.cwd()).
*/
const path = require('path');

const classNum = process.env.CLASS;
if (!classNum) {
  console.error('CLASS env var not set');
  process.exit(2);
}

const curriculumPath = path.join(process.cwd(), 'staticcurriculum', `class${classNum}`, 'curriculum.js');

let curriculum;
try {
  curriculum = require(curriculumPath);
} catch (err) {
  console.error('Error requiring', curriculumPath, err && err.message ? err.message : err);
  process.exit(3);
}

const subjectCount = Array.isArray(curriculum) ? curriculum.length : 0;
let chapterCount = 0;

if (Array.isArray(curriculum)) {
  curriculum.forEach(sub => {
    if (sub && Array.isArray(sub.chapters)) cha
